import { renderHook } from "@testing-library/react";
import { useTodos } from "../../hooks/useTodos";

describe('Pruebas en useTodos', () => {
    test('Debe', () => { 
        /*const { result } = renderHook( useTodos());
        const { todos, 
            totalTodos,
            handleNewTodo, 
            handleDeleteTodo, 
            handleToggleTodo,
            pendingTodosCount } = result.current;*/  
    
    });

});